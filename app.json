{
  "name": "Vonage API sample client application using Amazon Transcribe and Comprehend services ",
  "description": "This sample application gets Vonage Voice API PSTN calls to be transcribed and sentiment scored",
  "keywords": ["transcribe", "comprehend", "node.js", "amazon web services", "vonage"],
  "repository": "https://github.com/nexmo-se/transcribe-comprehend-client",
  "env":{
    "API_KEY": {
        "description": "Retrieve from https://dashboard.nexmo.com/settings",
        "required": true
    },
    "API_SECRET": {
        "description": "Retrieve from https://dashboard.nexmo.com/settings",
        "required": true
    },
    "APP_ID": {
        "description": "Retrieve from https://dashboard.nexmo.com/applications, click on your application",
        "required": true
    },
    "TRANSCRIBE_COMPREHEND_CONNECTOR_SERVER": {
    "description": "Your Transcribe & Comprehend connector server address",
    "value": "e.g. myserver.mycompany.com:40000 or xxxxxxx.herokuapp.com",
    "required": true
    },             
    "CALLEE_NUMBER": {
    "description": "Number to call when receiving incoming call",
    "value": "12995550101",
    "required": true
    }
  },  
  "buildpacks": [
    {
       "url": "heroku/nodejs"
    }
  ]
}